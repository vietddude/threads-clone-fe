export enum Privacy {
	PUBLIC = 'PUBLIC',
	PRIVATE = 'PRIVATE'
}

export enum NotificationType {
	ADMIN = 'ADMIN',
	LIKE = 'LIKE',
	REPLY = 'REPLY',
	FOLLOW = 'FOLLOW',
	REPOST = 'REPOST',
	QUOTE = 'QUOTE'
}

export enum PostPrivacy {
	FOLLOWED = 'FOLLOWED',
	ANYONE = 'ANYONE',
	MENTIONED = 'MENTIONED'
}
